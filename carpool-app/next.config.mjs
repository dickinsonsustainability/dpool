/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aaqcqgemyqwfqxlxxuzv.supabase.co','img.clerk.com'] // Make sure this is an array
    }
};

export default nextConfig;